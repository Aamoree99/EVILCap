name: Debug ENV Secret

on: [push]

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Print ENV secret to file
        run: |
          echo "${{ secrets.ENV }}" > env_secret.txt
          cat env_secret.txt
